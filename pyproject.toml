[project]
name = "sgc-rendezvous"
version = "0.0.1"
description = "WIP parking lot booking app for Constance"
dependencies = [
    "flask == 3",
    "gunicorn == 22",
]

[build-system]
requires = ["flit_core<4"]
build-backend = "flit_core.buildapi"

[tool.flit.module]
name = "app"
